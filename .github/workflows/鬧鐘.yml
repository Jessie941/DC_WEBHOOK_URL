
name: 鬧鐘助理

on:
  workflow_dispatch: # 允許手動觸發測試
  schedule:
    - cron: '25 14 * * *' # 每天 UTC 22:30 執行（等於台灣時間隔天早上 06:30）

jobs:
  check-sheet-and-alarm:
    runs-on: ubuntu-latest
    steps:
    - name: 複製程式碼
      uses: actions/checkout@v4.2.2

    - name: 設定 Python 環境
      uses: actions/setup-python@v5.4.0
      with:
        python-version: '3.11'

    - name: 安裝必要套件
      run: pip install requests

    - name: 執行定時鬧鐘
      env:
        DC_WEBHOOK_URL: ${{ secrets.DC_WEBHOOK_URL }}
      run: python app1.py
